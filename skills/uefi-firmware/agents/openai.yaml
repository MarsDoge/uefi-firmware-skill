interface:
  display_name: "UEFI Firmware"
  short_description: "UEFI specs, edk2 development, and debugging"
  brand_color: "#0F766E"
  default_prompt: "Use $uefi-firmware to explain this UEFI or edk2 firmware issue and point me to the right specification or source area."
policy:
  allow_implicit_invocation: true
